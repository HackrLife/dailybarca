name: daily-edition

on:
  schedule:
    - cron: "15 4 * * *"
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder for curation job
        run: |
          echo "Daily job hook is live."
          echo "Next: drop curated JSON into data/YYYY-MM-DD.json and update data/editions.json"
          echo "Commit on main and Vercel will publish the new edition."
